numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
xgboost>=2.0
matplotlib>=3.7

# Optional: enables the PyTorch backend for the v3 SC network.
# Without it, the SC network falls back to the dependency-free NumPy backend.
# torch>=2.0
